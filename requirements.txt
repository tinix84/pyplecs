# PyPLECS All Dependencies
# Meta-file that includes all requirement modules for backward compatibility
# For minimal installation, use requirements-core.txt instead

# Core functionality (required)
-r requirements-core.txt

# Desktop GUI automation (Windows only)
-r requirements-gui.txt

# Web UI and REST API
-r requirements-web.txt

# Advanced caching backends
-r requirements-cache.txt

# Note: Development tools are in requirements-dev.txt (install separately)
# Note: Optimization features are in requirements-opt.txt (install separately)
