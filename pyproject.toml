[tool.pydocstyle]
# Only check for presence of module/class/function docstrings in this pass.
select = ["D100", "D101", "D102"]
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyplecs"
version = "0.1.0"
description = "Advanced PLECS simulation automation with web UI and API"
readme = "README.md"
authors = [ { name = "riccardo tinivella", email = "tinix84@gmail.com" }]
license = { file = "LICENSE" }
requires-python = ">=3.8"

[tool.setuptools]
packages = ["pyplecs"]
include-package-data = true

[project.scripts]
pyplecs-setup = "pyplecs.cli.installer:main"
pyplecs-gui = "pyplecs.webgui:run_app"

[project.optional-dependencies]
# Optional extras users can install via: pip install .[parquet]
parquet = ["pyarrow>=5.0.0"]
yaml = ["pyyaml>=6.0"]
# Full feature set for convenience (parquet storage, yaml support)
full = ["pyarrow>=5.0.0", "pyyaml>=6.0"]
